<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg width="210mm" height="297mm" viewBox="0 0 210 297" version="1.1" xmlns="http://www.w3.org/2000/svg">
  <rect width="210" height="297" fill="white"/>
  <!-- Header with variant color -->
  <rect x="0" y="0" width="210" height="40" fill="{{ variant.style.color }}"/>
  <text x="20" y="25" font-family="Arial" font-size="12" fill="white">{{ title }}</text>
  <text x="20" y="60" font-family="Arial" font-size="10" fill="{{ variant.style.color }}">{{ subtitle }}</text>

  <!-- Features -->
  <text x="20" y="90" font-family="Arial" font-size="10" font-weight="bold" fill="#333">Features:</text>
  {% for feature in variant.features %}
  <text x="30" y="{{ 110 + loop.index0 * 16 }}" font-family="Arial" font-size="9" fill="#333">• {{ feature }}</text>
  {% endfor %}
</svg>
